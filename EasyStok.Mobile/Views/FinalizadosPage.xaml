<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EasyStok.Mobile.Views.FinalizadosPage"
             Title="Finalizados">
    <Grid Padding="22,16">
        <VerticalStackLayout Spacing="8" HorizontalOptions="Center" VerticalOptions="Center">
            <Label Text="Finalizados" Style="{StaticResource LabelHero}" HorizontalOptions="Center" />
            <Label Text="Em construcao (F5)" Style="{StaticResource LabelGreeting}" HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
