<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security config — substitui android:usesCleartextTraffic="true"
  do AndroidManifest. HTTPS obrigatorio em release; HTTP permitido apenas
  para dominios locais de desenvolvimento (emulador Android, localhost).
-->
<network-security-config>

    <!-- Defaults: HTTPS apenas. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Cleartext liberado apenas para emulador/localhost (dev). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

</network-security-config>
