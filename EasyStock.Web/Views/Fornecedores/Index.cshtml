@model FornecedoresListViewModel
@{
    ViewBag.Title = "Fornecedores";
    Layout = "_Layout";
}

<div class="flex items-center justify-between mb-6">
    <div class="pg-hd">
        <h1>Fornecedores</h1>
        <p>Gerencie seus fornecedores e pedidos de compra</p>
    </div>
    <div class="flex gap-3">
        <button type="button" x-data
                @@click="$dispatch('open-modal', 'novo-fornecedor')"
                class="px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-lg hover:bg-indigo-700">
            + Novo Fornecedor
        </button>
    </div>
</div>

<!-- Filtros -->
<div class="ws mb-6">
    <form method="get" class="flex gap-3 flex-wrap">
        <input type="text" name="search" value="@Model.Search" placeholder="Buscar fornecedor..."
               class="border border-slate-300 rounded-lg px-3 py-2 text-sm flex-1 min-w-48" />
        <div class="flex gap-2">
            @foreach (var (val, label) in new[] { ("", "Todos"), ("ativo", "Ativos"), ("inativo", "Inativos") })
            {
                var isActive = Model.FiltroStatus == val || (val == "" && Model.FiltroStatus == null);
                <a href="@Url.Action("Index", "Fornecedores", new { search = Model.Search, status = val == "" ? null : val })"
                   class="px-3 py-2 rounded-lg text-sm font-medium @(isActive ? "bg-indigo-600 text-white" : "bg-white border border-slate-300 text-slate-700 hover:bg-slate-50")">
                    @label
                </a>
            }
        </div>
        <button type="submit" class="px-4 py-2 bg-slate-100 border border-slate-300 rounded-lg text-sm hover:bg-slate-200">
            Buscar
        </button>
    </form>
</div>

<!-- Lista -->
<div class="ws">
    @if (!Model.Items.Any())
    {
        @await Html.PartialAsync("_EmptyState", null, new ViewDataDictionary(ViewData) {
            { "EmptyIcon", "🏭" },
            { "EmptyTitle", "Nenhum fornecedor cadastrado" },
            { "EmptyDescription", "Adicione seu primeiro fornecedor para organizar suas compras." },
            { "EmptyCta", "+ Novo Fornecedor" },
            { "EmptyCtaModal", "novo-fornecedor" }
        })
    }
    else
    {
        <div class="overflow-x-auto">
        <table class="tbl-full">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Tipo</th>
                    <th>Lead (dias)</th>
                    <th>Categorias</th>
                    <th>Status</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var f in Model.Items)
                {
                    <tr>
                        <td>
                            <div class="font-medium text-slate-900">@f.Nome</div>
                            @if (!string.IsNullOrEmpty(f.Email))
                            {
                                <div class="text-xs text-slate-500">@f.Email</div>
                            }
                        </td>
                        <td class="text-slate-600 text-sm capitalize">@f.Tipo</td>
                        <td class="text-slate-600">@(f.LeadTimeEstimadoDias?.ToString() ?? "—")</td>
                        <td class="text-slate-500 text-sm">@f.Categoria</td>
                        <td>
                            <span class="px-2 py-0.5 rounded-full text-xs font-medium @(f.Status == "ativo" ? "bg-green-100 text-green-700" : "bg-slate-100 text-slate-600")">
                                @f.Status
                            </span>
                        </td>
                        <td>
                            <a asp-action="Detail" asp-controller="Fornecedores" asp-route-id="@f.Id"
                               class="text-sm text-indigo-600 hover:text-indigo-700">Ver</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        </div>
    }
</div>

<!-- Modal novo fornecedor -->
<div x-data="{ open: false }" @@open-modal.window="open = ($event.detail === 'novo-fornecedor')"
     x-show="open" x-cloak
     class="fixed inset-0 z-50 flex items-center justify-center bg-black/40"
     @@click.self="open = false">
    <div class="bg-white rounded-xl shadow-xl w-full max-w-lg p-6 overflow-y-auto max-h-screen">
        <h2 class="text-lg font-semibold text-slate-900 mb-4">Novo Fornecedor</h2>
        <form asp-action="Criar" asp-controller="Fornecedores" method="post">
            @Html.AntiForgeryToken()
            <div class="grid grid-cols-2 gap-3 mb-3">
                <div class="col-span-2">
                    <label class="block text-sm font-medium text-slate-700 mb-1">Nome *</label>
                    <input type="text" name="nome" required
                           class="w-full border border-slate-300 rounded-lg px-3 py-2 text-sm" />
                </div>
                <div>
                    <label class="block text-sm font-medium text-slate-700 mb-1">CNPJ/CPF</label>
                    <input type="text" name="documento" class="w-full border border-slate-300 rounded-lg px-3 py-2 text-sm" />
                </div>
                <div>
                    <label class="block text-sm font-medium text-slate-700 mb-1">Responsável</label>
                    <input type="text" name="contato" class="w-full border border-slate-300 rounded-lg px-3 py-2 text-sm" />
                </div>
                <div>
                    <label class="block text-sm font-medium text-slate-700 mb-1">Email</label>
                    <input type="email" name="email" class="w-full border border-slate-300 rounded-lg px-3 py-2 text-sm" />
                </div>
                <div>
                    <label class="block text-sm font-medium text-slate-700 mb-1">Telefone</label>
                    <input type="text" name="telefone" class="w-full border border-slate-300 rounded-lg px-3 py-2 text-sm" />
                </div>
                <div>
                    <label class="block text-sm font-medium text-slate-700 mb-1">Tipo *</label>
                    <select name="tipo" required class="w-full border border-slate-300 rounded-lg px-3 py-2 text-sm">
                        <option value="nacional">Nacional</option>
                        <option value="importado">Importado</option>
                        <option value="dropshipping">Dropshipping</option>
                    </select>
                </div>
                <div>
                    <label class="block text-sm font-medium text-slate-700 mb-1">Lead Time (dias)</label>
                    <input type="number" name="leadTimeEstimadoDias" value="7" min="0"
                           class="w-full border border-slate-300 rounded-lg px-3 py-2 text-sm" />
                </div>
                <div class="col-span-2">
                    <label class="block text-sm font-medium text-slate-700 mb-1">Categorias</label>
                    <input type="text" name="categoria" placeholder="Ex: eletrônicos, roupas..."
                           class="w-full border border-slate-300 rounded-lg px-3 py-2 text-sm" />
                </div>
                <div class="col-span-2">
                    <label class="block text-sm font-medium text-slate-700 mb-1">Observações</label>
                    <textarea name="observacoes" rows="2"
                              class="w-full border border-slate-300 rounded-lg px-3 py-2 text-sm"></textarea>
                </div>
            </div>
            <div class="flex justify-end gap-3 pt-3 border-t">
                <button type="button" @@click="open = false"
                        class="px-4 py-2 text-sm text-slate-700 border border-slate-300 rounded-lg hover:bg-slate-50">
                    Cancelar
                </button>
                <button type="submit" x-data="{ loading: false }" @@click="loading = true"
                        :disabled="loading" :class="{ 'opacity-50 cursor-not-allowed': loading }"
                        class="px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-lg hover:bg-indigo-700">
                    <span x-show="!loading">Criar Fornecedor</span>
                    <span x-show="loading" x-cloak class="flex items-center gap-2"><span class="spinner"></span> Salvando...</span>
                </button>
            </div>
        </form>
    </div>
</div>
