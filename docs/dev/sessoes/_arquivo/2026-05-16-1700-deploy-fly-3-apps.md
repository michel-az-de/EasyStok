# Sessao deploy-fly-3-apps

Data: 2026-05-16 17:00 BRT (sessao ~14:00-17:15)
Worktree: master direto (branches dedicadas para os fixes)
Identidade Git: felipe.azevedo@gmail.com / michel-az-de
Status final: completo

## O que foi feito

1. **Inventario inicial conforme protocolo CLAUDE.md secao 0.** Estado:
   master sincronizado (0/0), build verde, 6 worktrees ativos.
   Working tree "dirty" em 2 JS de `EasyStock.Web/wwwroot/etiqueta/`
   resultou ser falso positivo (apenas EOL CRLF<->LF, diff real vazio).

2. **Auditoria de testes (R8).** Achei 2 falhas nao catalogadas em
   flaky-tests.md:
   - `ItemEstoqueTests.cs:89` esperava `StatusItemEstoque.Critical`
     mas codigo produz `Esgotado` ao zerar (consistente com nome do
     teste).
   - `LoteUseCasesTests.cs:110` nao passava `ProdutoId`, mas
     `CriarLoteUseCase` ganhou validacao R1 obrigando vinculo a
     produto (RDC 727/2022).

   PR #148 `fix(tests): atualiza assercoes desatualizadas em
   ItemEstoque e CriarLote` — branch + push + squash-merge admin.
   Master pulou de 08091d8f para 68b924fb.

3. **App fantasma `easystok-udggng` deletado** via
   `fly apps destroy -y` (zero maquinas, criacao abortada).

4. **Deploy API** (`fly deploy --config fly.toml -a easystok`):
   v41 -> v42. Health check passou. `GET https://easystok.fly.dev/health/live`
   retorna `200 Healthy`.

5. **Deploy Web** (1a tentativa): subiu v13 -> v14 mas `GET /` deu
   500 com `AmbiguousMatchException`. Investigado:
   `DashboardController.Index` reivindicava `[HttpGet("/")]` E
   `[HttpGet("/dashboard")]`, colidindo com `SiteController.Index`
   que tem `[Route("/")]`. Bug introduzido em d6281e82
   `feat(dashboard): refatoracao completa com analytics avancado`
   (2026-05-15 17:47 BRT) — **ja estava em prod desde v13**, so nao
   tinha sido detectado.

   PR #149 `fix(web): remove rota / duplicada do DashboardController`
   — branch + push + squash-merge admin. Master 68b924fb -> 1a9de62b.

   **Redeploy Web**: v14 -> v15. `GET https://easystok-web.fly.dev/`
   retorna `200`, landing publica `<title>Estoque vivo em 7 dias —
   EasyStok</title>` renderiza ok.

6. **Deploy Admin** (`fly deploy --config fly.admin.toml -a
   easystok-admin`): v6 -> v7. `GET /` retorna `302 -> /login`
   (esperado, comentado no fly.admin.toml). Seguindo redirect:
   `200` com `<title>Login — EasyStock Admin</title>`.

## O que ficou pendente

- **Worker** nao tem fly.toml — Render ainda hospeda. Nao foi pedido
  no escopo. Se entrar no Fly, criar `fly.worker.toml` e usar
  `EasyStock.Worker/Dockerfile` ja existente.
- **2 JS dirty** em `EasyStock.Web/wwwroot/etiqueta/` continuam com
  warning de EOL no `git status`. Conteudo identico ao HEAD. Pode
  ser normalizado num `git add --renormalize .` em sessao futura
  dedicada a higiene EOL.
- **PollingOutboxSignalerTests.WaitAsync_completa_quando_intervalo_passa**
  segue flaky tolerado (R8 + flaky-tests.md).
- **Architecture test `Exceptions_De_Domain_Devem_Ficar_No_Domain`**
  segue falhando (catalogado em flaky-tests.md como regressao
  arquitetural nao-corrigida — decisao Felipe).
- **App fantasma residual**: nenhum agora (`easystok-udggng`
  destruido). Quatro apps no Fly: easystok, easystok-web,
  easystok-admin (todos started + healthy).

## Decisoes tomadas

- **Fix de testes antes do deploy** (em vez de pular ou registrar
  como flaky). Razao: 1 linha + 1 metodo de setup, sem risco.
- **Fix do bug de rota / no Web no meio do deploy** (em vez de
  rollback para v13 ou seguir Admin com Web broken). Razao: v13
  ja tinha o mesmo bug — rollback nao resolveria. Fix de 1 linha.
- **Branch + PR + squash-merge --admin** para ambos os fixes,
  conforme R1 do CLAUDE.md v2.0. Nada de commit direto em master.

## Commits criados

- 68b924fb fix(tests): atualiza assercoes desatualizadas em
  ItemEstoque e CriarLote (#148, squash-merged)
- 1a9de62b fix(web): remove rota / duplicada do DashboardController
  (#149, squash-merged)

## Branches criadas/deletadas

- `fix/stale-tests-itemestoque-lote` — criada, mergeada, deletada
  (remote) via `--delete-branch`.
- `fix/web-dashboard-route-conflict` — criada, mergeada, deletada
  (remote) via `--delete-branch`.

## Estado Fly pos-sessao

| App | Versao | Status | Smoke |
|-----|--------|--------|-------|
| easystok (API) | v42 | started, 1 check passing | `/health/live` -> 200 Healthy |
| easystok-web | v15 | started | `/` -> 200, landing renderiza |
| easystok-admin | v7 | started | `/` -> 302 -> /login -> 200, tela login renderiza |
| ~~easystok-udggng~~ | — | destroyed | — |

## Proxima acao recomendada

- Validacao manual UI: navegar pelo Web e Admin em prod, batendo
  rotas chave (dashboard, pedidos, estoque, lotes, helpdesk),
  porque so o smoke `/` foi testado.
- Considerar mover Worker para Fly (atualmente Render, auto-deploy
  OFF). Avaliar quando o billing Render apertar novamente.
- Re-rodar suite de testes completa numa sessao dedicada a
  destravar `Exceptions_De_Domain_Devem_Ficar_No_Domain` (catalogado
  como pendente em flaky-tests.md).

## Referencias

- Plano: ad-hoc (revisao + deploy, pedido em mensagem unica do Felipe)
- ADRs: nenhum tocado nesta sessao
- Incidentes: nenhum aberto. O bug `/ AmbiguousMatch` foi caught em
  smoke pos-deploy, nao causou incidente em prod (PR #149 fechou
  antes de ser exposto a usuarios reais — o Web v14 viveu ~2 min)
- PRs: #148, #149 (ambos squash-merged em master)
